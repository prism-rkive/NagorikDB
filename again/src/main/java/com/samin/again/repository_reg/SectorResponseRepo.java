package com.samin.again.repository_reg;

import com.samin.again.entity.SectorResponseTeam;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SectorResponseRepo extends JpaRepository<SectorResponseTeam,String> {
}