package com.samin.again.models;

public enum Status {
    SUBMITTED, // When the user submits the emergency
    ASSIGNED,  // When a rescue team is assigned
    RESCUED,   // When the emergency is resolved
    DELAYED
}
